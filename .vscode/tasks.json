{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run App",
			"command": "${command:workbench.action.debug.start}",
			 //"type": "shell",
			//"command": "source venv/bin/activate && python3 ${file}",
			"group": {
				"kind": "build",
				"isDefault": true
			},
		}
	]
}